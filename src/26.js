function createRandomNodejsCode() {
  // Example: Generate some basic code
  const randomCode = `
    function add(a, b) {
      return a + b;
    }
    console.log(add(2, 3));
  `;
  
  return randomCode;
}
