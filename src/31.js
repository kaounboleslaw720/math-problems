function getRandomNodejsCode() {
  // Simulate generating a random piece of code
  const randomCode = `
    function getRandomInt(min, max) {
      return Math.floor(Math.random() * (max - min + 1)) + min;
    }

    let myName = "Alice";
    console.log(`Hello, ${myName}!`);
  `;
  
  return randomCode;
}
